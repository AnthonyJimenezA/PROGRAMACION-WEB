@model IEnumerable<Proyecto_1__CRUD.Models.Cliente>

@{
    ViewData["Title"] = "Clientes";
}

<h1 class="card-title mb-4">Clientes</h1>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show mb-3 col-md" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="mb-3 d-flex justify-content-between">
    <div class="col-3">
        <form method="get" asp-action="Index" id="searchForm">
            <div class="input-group">
                <input type="text" name="searchTerm" id="searchInput" class="form-control form-control-sm" placeholder="Buscar por Cédula" />
                <button type="submit" class="btn btn-primary btn-sm">Buscar</button>
            </div>
        </form>
    </div>
    <a asp-action="Create" class="btn btn-primary">Crear Nuevo</a>
</div>

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Cédula</th>
                <th>Nombre Completo</th>
                <th>Provincia</th>
                <th>Cantón</th>
                <th>Distrito</th>
                <th>Direccion exacta</th>
                <th>Invierno</th>
                <th>Verano</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Identificacion)</td>
                    <td>@Html.DisplayFor(modelItem => item.NombreCompleto)</td>
                    <td>@Html.DisplayFor(modelItem => item.Provincia)</td>
                    <td>@Html.DisplayFor(modelItem => item.Canton)</td>
                    <td>@Html.DisplayFor(modelItem => item.Distrito)</td>
                    <td>@Html.DisplayFor(modelItem => item.DireccionExacta)</td>
                    <td>@Html.DisplayFor(modelItem => item.PreferenciaInvierno)</td>
                    <td>@Html.DisplayFor(modelItem => item.PreferenciaVerano)</td>
                    <td class="flex-cell">
                        <a href="@Url.Action("Edit", new { identificacion = item.Identificacion })" class="icon-action" title="Editar">
                            <i class="fas fa-edit fa-lg"></i>
                        </a>
                        <a href="@Url.Action("Delete", new { identificacion = item.Identificacion })" class="icon-action-delete" title="Eliminar">
                            <i class="fas fa-trash-alt fa-lg"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
